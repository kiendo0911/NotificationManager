# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NotificationManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NotificationManager
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'
  pod 'Kingfisher', '~> 7.0'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'RxDataSources', '~> 5.0'
  
  target 'NotificationManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NotificationManagerUITests' do
    # Pods for testing
  end

end
